# InsightUX 真实演示脚本

本脚本只演示当前系统已经实现的能力，不使用预置静态会话或虚构的 AI 结果。建议提前配置 Qwen；未配置时应主动说明系统正在使用“本地规则引擎”降级。

## 演示前准备

```bash
cp .env.example .env
npm install
npm run dev
```

修改 `.env` 中的管理员密码。打开分析端 `http://localhost:5173/#/` 并登录，同时准备另一个浏览器窗口或隐私窗口作为被试端。

## 0:00–0:40 创建任务

1. 进入“任务管理”。
2. 点击“新建任务”。
3. 输入任务名称、被试说明和三个任务步骤。
4. 选择“创建后立即发布”。
5. 复制系统生成的 HashRouter 测试链接。

讲解重点：任务、Token 和发布状态都保存在 SQLite；暂停任务后，原链接将不能再创建会话。

## 0:40–1:40 被试完成测试

1. 在被试窗口打开刚复制的链接。
2. 展示来自任务本身的说明和步骤。
3. 阅读知情同意：行为事件、面部特征和低清缩略图均被准确说明，不采集麦克风或连续原始视频。
4. 允许摄像头，等待真实 MediaPipe 检测通过。
5. 面部保持在框内，完成连续校准。
6. 进入结算页，处理 3.5 秒后出现的优惠券，再提交订单。

可补充演示：拒绝摄像头后，系统不会伪造检测成功，而是要求被试明确选择“仅记录页面行为”。

## 1:40–2:30 会话回放与指标

1. 返回分析端“会话列表”并点击刷新。
2. 展示刚完成的唯一参与者编号、真实时长、事件数和面部帧数。
3. 进入深度分析页并播放 rrweb 回放。
4. 拖动实际时长时间轴，观察面部缩略图与压力曲线同步。
5. 指出总时长、首次点击、犹豫时间、鼠标距离、方向反转和真实最终选择。

讲解重点：分析页只读取当前路由会话的服务端数据，不会回退到其他会话或固定 20 秒示例。

## 2:30–3:10 智能诊断

1. 点击“开始智能诊断”。
2. 查看诊断提供者标签：`Qwen 多模态` 或 `本地规则引擎`。
3. 展示严重程度、可复现置信度、根因、带时间戳证据链和建议。

如果 Qwen 请求失败，界面会明确显示降级原因；不要把规则报告讲成模型输出。

## 3:10–3:40 报告与仪表盘

1. 打开完整报告，确认日期、指标和结论都来自当前会话。
2. 点击“分享链接”，在未登录窗口打开只读报告。
3. 点击“打印 / 导出 PDF”展示浏览器打印版式。
4. 返回仪表盘切换 Admin 全局视图，展示真实会话趋势和严重程度分布。

## 收尾表述

> InsightUX 把任务发布、无感采集、同步回放、可追溯诊断和报告分享连成一条可私有部署的真实链路；模型不可用时系统也会透明降级，不用虚构 AI 结果维持演示。
